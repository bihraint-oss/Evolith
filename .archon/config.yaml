# Evolith Project Configuration
# Mixed provider strategy: Codex (complex/building) + GLM (simple/reviewing)
# Builder (Codex) ≠ Reviewer (GLM) — different models for different perspectives

assistant: codex

assistants:
  codex:
    model: gpt-5.4
    modelReasoningEffort: xhigh
  claude:
    # Routes to GLM-5.1 via Anthropic API proxy (all Claude models → GLM-5.1)
    # Used for: classification, review, validation, synthesis, reporting
    model: sonnet

defaults:
  loadDefaultCommands: true
  loadDefaultWorkflows: true
